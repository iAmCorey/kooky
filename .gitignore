# Swift / SPM
.build/
.swiftpm/
Packages/
Package.resolved

# Xcode
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
build/
xcuserdata/

# macOS
.DS_Store
._*

# Editor
.vscode/
.idea/
*.swp
*~

# Misc
.netrc

# Signing / notarization credentials — never commit, never recoverable once public
*.p12
*.cer
*.pem
AuthKey_*.p8
*.mobileprovision
.env
.env.*

# Claude Code local state — settings, worktrees, scratch (per-machine, not for shared repo)
.claude/

# Vendored prebuilt binaries — fetched via scripts/setup-libghostty.sh on first build
Vendor/

# .app bundle output (build-app.sh)
dist/

# Local AI agent context — not for public history
CLAUDE.md
AGENTS.md
AGENT.md
GEMINI.md
.cursorrules
.cursor/
.codex/
.gemini/
.opencode/
.kiro/
.factory/
.windsurf/
.roo/
.junie/
.aider*
.mcp.json

# Private design drafts — not for public history
docs/rust-rewrite-design.md
IDEAS.md
.clinerules
.windsurfrules
.continue/

# Antigravity CLI runtime state (project-scoped symlinks into ~/.gemini)
.antigravitycli/
bench-history.jsonl
